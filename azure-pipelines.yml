# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: Default
  demands: 
    - MSBuild
    - Nuget


stages:
  - stage: Akram2s
    jobs:
      - job: Write
        steps:
          - script: echo $(Agent.Name)
          - bash: $(Build.SourcesDirectory)/scripts/ss.sh

    


